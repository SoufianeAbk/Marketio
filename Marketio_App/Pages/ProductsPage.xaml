<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Marketio_App.Pages.ProductsPage"
             Title="Producten">

    <ContentPage.Resources>
        <Color x:Key="LightBackgroundColor">#F5F5F5</Color>
        <Color x:Key="DarkBackgroundColor">#1F1F1F</Color>
        <Color x:Key="LightHeaderBackground">#FFFFFF</Color>
        <Color x:Key="DarkHeaderBackground">#2A2A2A</Color>
        <Color x:Key="LightTextColor">#000000</Color>
        <Color x:Key="DarkTextColor">#FFFFFF</Color>
        <Color x:Key="LightSecondaryText">#666666</Color>
        <Color x:Key="DarkSecondaryText">#CCCCCC</Color>
        <Color x:Key="LightPlaceholder">#999999</Color>
        <Color x:Key="DarkPlaceholder">#666666</Color>
        <Color x:Key="LightPickerBackground">#F0F0F0</Color>
        <Color x:Key="DarkPickerBackground">#3A3A3A</Color>
        <Color x:Key="LightEmptyViewText">#000000</Color>
        <Color x:Key="DarkEmptyViewText">#FFFFFF</Color>
        <Color x:Key="LightEmptyViewSubText">#666666</Color>
        <Color x:Key="DarkEmptyViewSubText">#CCCCCC</Color>
        <Color x:Key="SuccessGreen">#34C759</Color>
        <Color x:Key="LightStockText">#999999</Color>
        <Color x:Key="DarkStockText">#AAAAAA</Color>
    </ContentPage.Resources>

    <Grid RowDefinitions="Auto,*" Padding="0" BackgroundColor="{AppThemeBinding Light={StaticResource LightBackgroundColor}, Dark={StaticResource DarkBackgroundColor}}">

        <!-- Header with Search and Filter -->
        <VerticalStackLayout 
            Padding="16" 
            Spacing="12" 
            BackgroundColor="{AppThemeBinding Light={StaticResource LightHeaderBackground}, Dark={StaticResource DarkHeaderBackground}}">

            <Label 
                Text="Producten"
                FontSize="24"
                FontAttributes="Bold"
                TextColor="{AppThemeBinding Light={StaticResource LightTextColor}, Dark={StaticResource DarkTextColor}}" />

            <!-- Search Bar -->
            <SearchBar 
                Placeholder="Zoeken naar producten..."
                Text="{Binding SearchQuery}"
                CancelButtonColor="#007AFF"
                PlaceholderColor="{AppThemeBinding Light={StaticResource LightPlaceholder}, Dark={StaticResource DarkPlaceholder}}" />

            <!-- Category Filter Picker -->
            <Picker 
                Title="Selecteer categorie"
                SelectedItem="{Binding SelectedCategory}"
                FontSize="14"
                TextColor="{AppThemeBinding Light={StaticResource LightTextColor}, Dark={StaticResource DarkTextColor}}"
                BackgroundColor="{AppThemeBinding Light={StaticResource LightPickerBackground}, Dark={StaticResource DarkPickerBackground}}" />

        </VerticalStackLayout>

        <!-- Error Message -->
        <Frame 
            Grid.Row="1"
            BorderColor="Red"
            CornerRadius="8"
            Padding="12"
            Margin="16"
            IsVisible="{Binding ErrorMessage, Converter={StaticResource IsNotNullOrEmptyConverter}}"
            HasShadow="True">
            <Label 
                Text="{Binding ErrorMessage}"
                TextColor="White"
                BackgroundColor="#FF6B6B"
                FontSize="12" />
        </Frame>

        <!-- Products Grid or Empty State -->
        <RefreshView 
            Grid.Row="1"
            Command="{Binding RefreshProductsCommand}"
            IsRefreshing="{Binding IsRefreshing}">

            <CollectionView 
                ItemsSource="{Binding Products}"
                SelectionMode="Single"
                SelectionChangedCommand="{Binding SelectProductCommand}">

                <CollectionView.ItemsLayout>
                    <GridItemsLayout 
                        Orientation="Vertical"
                        HorizontalItemSpacing="12"
                        VerticalItemSpacing="12" />
                </CollectionView.ItemsLayout>

                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <StackLayout Padding="12" Spacing="8">
                            <Frame 
                                CornerRadius="12"
                                Padding="0"
                                HasShadow="True"
                                BorderColor="Transparent">
                                <Image 
                                    Source="{Binding ImageUrl, Converter={StaticResource ImageUrlConverter}}"
                                    Aspect="AspectFill"
                                    HeightRequest="150" />
                            </Frame>

                            <Label 
                                Text="{Binding Name}"
                                FontSize="14"
                                FontAttributes="Bold"
                                TextColor="{AppThemeBinding Light={StaticResource LightTextColor}, Dark={StaticResource DarkTextColor}}"
                                LineBreakMode="TailTruncation" />

                            <Label 
                                Text="{Binding Description}"
                                FontSize="12"
                                TextColor="{AppThemeBinding Light={StaticResource LightSecondaryText}, Dark={StaticResource DarkSecondaryText}}"
                                LineBreakMode="TailTruncation" />

                            <StackLayout Orientation="Horizontal" Spacing="8">
                                <Label 
                                    Text="{Binding Price, StringFormat='€{0:F2}'}"
                                    FontSize="14"
                                    FontAttributes="Bold"
                                    TextColor="{StaticResource SuccessGreen}" />
                                <Label 
                                    Text="{Binding Stock, StringFormat='Voorraad: {0}'}"
                                    FontSize="12"
                                    TextColor="{AppThemeBinding Light={StaticResource LightStockText}, Dark={StaticResource DarkStockText}}" />
                            </StackLayout>
                        </StackLayout>
                    </DataTemplate>
                </CollectionView.ItemTemplate>

                <CollectionView.EmptyView>
                    <VerticalStackLayout Spacing="16" Padding="20" HorizontalOptions="Center" VerticalOptions="Center">
                        <Label 
                            Text="Geen producten gevonden"
                            FontSize="18"
                            FontAttributes="Bold"
                            TextColor="{AppThemeBinding Light={StaticResource LightEmptyViewText}, Dark={StaticResource DarkEmptyViewText}}"
                            HorizontalOptions="Center" />
                        <Label 
                            Text="Probeer uw zoekopdracht aan te passen"
                            FontSize="14"
                            TextColor="{AppThemeBinding Light={StaticResource LightEmptyViewSubText}, Dark={StaticResource DarkEmptyViewSubText}}"
                            HorizontalOptions="Center" />
                    </VerticalStackLayout>
                </CollectionView.EmptyView>
            </CollectionView>

        </RefreshView>

        <!-- Loading Indicator -->
        <ActivityIndicator 
            Grid.Row="1"
            IsRunning="{Binding IsLoading}"
            IsVisible="{Binding IsLoading}"
            Color="#007AFF"
            VerticalOptions="Center"
            HorizontalOptions="Center" />

    </Grid>

</ContentPage>